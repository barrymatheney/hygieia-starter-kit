<settings>
    <pluginGroups>
        <pluginGroup>org.sonarsource.scanner.maven</pluginGroup>
    </pluginGroups>


    <profiles>
        <profile>
            <id>sonar</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <!-- Optional URL to server. Default value is http://localhost:9000 -->
                <sonar.host.url>
                  http://sonarqube:9000
                </sonar.host.url>
            </properties>
        </profile>
    </profiles>

    <servers>
        <server>
            <!-- this is for github https-->
             <id>my-scm-server</id> 
             <username>barrymatheney</username> 
            <password>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</password> 
                <!--             <privateKey>/root/.ssh/id_rsa</privateKey>  -->
        </server>
    </servers>

</settings>